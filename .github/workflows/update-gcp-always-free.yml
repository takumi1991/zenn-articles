name: Update GCP Always Free Article

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
    - cron: "5 0 1 * *"

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run update script
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
          APIFY_ACTOR_ID_GCP: ${{ secrets.APIFY_ACTOR_ID_GCP }}
        run: node scripts/update-gcp.js

      - name: Check output
        run: |
          echo "data.json content:"
          cat data.json

      - name: Generate markdown
        run: |
          mkdir -p articles

          # FrontMatter
          cat <<'EOF' > articles/gcp-always-free.md
---
title: "Google Cloud Always Free"
emoji: "☁️"
type: "tech"
topics: ["gcp", "cloud"]
published: true
---
EOF

          echo "" >> articles/gcp-always-free.md
          echo "# Google Cloud Always Free" >> articles/gcp-always-free.md

          DATE=$(jq -r '.fetchedAt' data.json)
          echo "" >> articles/gcp-always-free.md
          echo "最終更新日: ${DATE}" >> articles/gcp-always-free.md
          echo "" >> articles/gcp-always-free.md

          jq -c '.items[]' data.json | while read item; do
            TITLE=$(echo "$item" | jq -r '.title')
            DESC=$(echo "$item" | jq -r '.description')
            FREE=$(echo "$item" | jq -r '.free_tier')
            LINK=$(echo "$item" | jq -r '.link')

            cat <<EOF >> articles/gcp-always-free.md
## 🌟 ${TITLE}

${DESC}

**無料枠**：${FREE}  
${LINK}

---

EOF
          done

      - name: Commit and push
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add articles/gcp-always-free.md
          git commit -m "Update GCP Always Free article" || echo "No changes"
          git push
